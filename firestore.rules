rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows any logged-in user to read and write to the database
      allow read, write: if true;
    }
  }
}
